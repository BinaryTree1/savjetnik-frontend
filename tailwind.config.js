/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Specify files where Tailwind classes are used
  ],
  theme: {
    extend: {}, // Extend the default theme
  },
  plugins: [], // Add plugins here
};
